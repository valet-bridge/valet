- reckon.pl to run clean as is
- Turn || errors back on, eliminate
- Player numbers (e.g. Poland) find by regex
- All known names should parse into first, initial, particle, last
- No inconsistencies
- In sublines, look for cases where nothing actually gets modified.
- Split out remaining code in reckon.pl, clean up

Consolidate
- CITY and EMAIL_CITY
- COUNTRY and EMAIL_COUNTRY
- REGION and EMAIL_REGION
- UNIVERSITY and EMAIL_UNIVERSITY
- SYSTEM: Probably SYSTEM AND EMAIL_SYSTEM
  - EMAIL_SYSTEM is actually solid
  - SYSTEM could perhaps still be a name, esp. early on
  - Look in SYSTEM first for system words, then for names
  - Take some kind of vote, claw back some as names
- PRIVATE
  - Depends on presence of EMAIL and LEVEL
    - Both present: If one is the same, delete
    - Otherwise flag?
    - EMAIL present: LEVEL (happens?)
    - LEVEL present: EMAIL_PRIVATE (happens?)
    - None present: EMAIL_PRIVATE
- MAGIC and CODE don't matter (should be at the end)
- USER_ONE and USER_TWO: Try to match with names
- OPEN
  - Segregate out high charsets for now (later use COUNTRY)
  - Hex-looking strings
  - Similar to SYSTEM vetting: words, votes
  - Flag conflict with USER_FIRST, INITIALS, PARTICLES, LAST
  - Eliminate USER_ONE and USER_TWO if matching

Regenerate Both.pm
Probably have to generate a presumption that it's one or the other.
First + Both(Last) = First + Last
Both(First) + Last = First + Last
Count against EBL and BBO occurrences.

---

* Read all Tournaments, or the specific country we're investigating
  - Can include a wbf list of numbers

* Read the output of digest.pl
  - By TNAME
  - Can recover header, chapters...
  - Skip onles that are not in Tournaments

* Read a file of BBO lin names (.lin and .ref parsed)
  - Read the most recent number from a directory
  - In the rare cases with more than one pn line, flag
  - Can be w:12345 rather than a name
  - Be able to write to the next higher-numbered file

* Read the names file corresponding to wbf numbers (should only be
  one file even if multiple numbers), or if not present, the master
  names file
  - Hash by names
  - Also by country/countries of origin

* Read the boards file of each wbfno
  - Keep track of round, board, pairs
  - Can associate to country if a national tournament
  - Or to team name in general?

* Loop over Tournaments
  - Loop over Edition
    - Skip unless wbfno
    - Tabulate players and their partnerso

* Interactive loop
  - stats: Table such as

        Total   Matched     %
Files
Names

  - changes: Number of changes in this invocation
  - tournaments: Numbered list of tournaments
  - active: Skip the OK ones
  - players match-string [makes list; these are EBL names]
  - tags match-string [these are unrecognized ones]
    - When selecting, get the tournaments and partners for each
    - Automatic suggestion as below
    - Ability to fix and prompt for next name

                Files   Matched         Names   Matched         OK?
Tournament1
Tournament2

  - select [tno]
  - editions; Similar tables
  - active: Skip the OK ones
  - select [eno or ename]
  - suggest (gives top proposal)
  - missing (list lin numbers with 1+ missing players,
    try to guess from the results files who they were,
    show that rest of table makes sense, including NS/EW orientation
    within pairs ideally)

String          meckstroth
Teams           100 USA
                  2 sth else
Partners        100 x (string or WBF + string)
                  4 y

Candidates by similarity

Jeff MECKSTROTH
(his profile)
General partnerships

Match only if age, gender suitable?  Or give all, prioritized
                
Fix? [Ynq]

This recalculates, keeps track of # changes, and resuggests next
that hasnt' been n'ed already

When q, back out to list of editions (table, select prompt)

When q, back to list of tournaments

w: Write file, reset stats
