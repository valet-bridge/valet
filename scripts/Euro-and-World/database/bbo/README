Consistencies:
- Dates
  - In Tname and Meet separately
    - is a date
    - Must have a and b
    - max 14 days apart, a <= b, YEAR matches if present
    - Tag is either year (if present) or year ABD or year-yy
  - Add a Tname meet tag if not same as Tname tag
  - Both Tname and Meet present
    - Meet tag present as expected
    - date1 >= date1, date2 <= date2, year matches if present
    - Look for Tname that could be Meet but isn't
- Names of Tname and Meet separately
  - Same list for each file (no extras)
- Usage
  - All chapters of Tname used (maybe up to 2016 or in interval)
  - Dates in Tname as tight as possible
  - Field values are known for ORG, COUNTRY, ZONE, REGION etc.
  - If set, they match, e.g. city and country
- Counters
  - Chapters with counters in data, but not set
  - All same STAGE in chapter if STAGE is present

Fields
- Move YEAR up to above CHAPTERS
- 15847 IMP, SCORING
- 46289 EVENT_ORIGIN
- 46284 TITLE_SPONSOR
- 45865 MEET ought to be up in the title
- 45785 two TITLE_CITY?
- In general not two of anything meaningful


- Output a spreadsheet, look at org, country, origin, zone

Fields
- All fields used
- Move fields up
- Final form of output, e.g. TITLE_MEET,
  TOURNAMENT_CITY
- TITLE_MEET comes out if MEET, e.g. 3620 Pula Bridge Festival
- Stats of fields used at different levels; avg # fields?
- Italian Mixed Pairs 2014: YEAR stays at BBONO level

In some cases like Final, could perhaps pull up the team names
to the chapter level in the output!  That might mean splitting
the SF too.

Check manually per country file

---

Should we already map Juniors to U26?

Philadelphia Solomon Teams is an odd Tname

22029, not all TEAM1_FORM and TEAM2_FORM go away?

Cornell in Australian tournaments is not Cornell University, e.g.
27126

VERBOSE flag in RegCounter, for new countries or datasets.

Store pdf's showing also player names

Scrapers:
Maybe store scraped pdf's of URL's when available?

* Activate KDEBUG in Knowledge.pm
* Can we limit TWORD to actual multi-purpose tournament and not
  single words?

More general stuff
------------------

Check the .ref files that modify qx against boards, lines
(Output #lines from C++ as well?)
